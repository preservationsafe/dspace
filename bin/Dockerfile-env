#
# DSpace v6.x Dockerfile, using ubuntu 16.04, oracle jdk7
#
# https://vitae/....
#

# Pull base image, ubuntu 16.04 + oracle jdk7.0.80 + tomcat 7.0.x
FROM dockerepo.library.arizona.edu:5000/dspace6-dev:__DSPACE_BUILD_TAG__

# Update dspace environment config values
RUN cd /opt/tomcat/dspace && bin/overlay-config.pl __DSPACE_STAGE_ENV__ dspace-install/config
